# Daily Log - 2026-04-05 (Sun)

## Daily Self-Improvement Audit (9:30 PM UTC / 2:30 PM PT)

Paperclip API write path was unavailable during the audit window.
- POST /api/companies/.../issues -> Cloudflare 502
- POST /api/issues/.../comments -> Cloudflare 502
- GET skill marketplace also returned 502

### 3 improvement ideas prepared (not filed due Paperclip outage)

1. **Harden raw CDP scraper library**
   - Fix `regal-cdp-scrape.py` tuple/unpack bug from Apr 4
   - Promote reusable raw-CDP helper for both Regal and CorePower
   - Reliability-critical because raw CDP succeeded while mcporter was offline

2. **Failure artifact capture**
   - Save URL, scrape path, snapshot/DOM excerpt, parser input/output, and timestamp on every failure
   - Speeds debugging when layouts change or parsers break

3. **Add AMC Universal CityWalk backup coverage**
   - Expand movie coverage beyond Regal Sherman Oaks
   - Surface alternate showtimes / premium formats when Regal is sold out or weak

### Intended top pick escalation
- Top pick: Harden raw CDP scraper library
- Rationale: Apr 4 proved MCP can be down while raw CDP still works, but the helper script is buggy and required an inline workaround
- Could not escalate to Clawd because Paperclip comments were also returning 502
