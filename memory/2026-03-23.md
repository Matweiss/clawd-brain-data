## 2026-03-23 Backup Snapshot

Created a restore-grade backup snapshot under `backup-snapshots/2026-03-23/` to preserve current OpenClaw state before any potential VPS/container reset.

### Preserved
- Workspace memory and handoff docs
- Mission Control project
- clawd-brain-data historical repo copy
- Sarah agent bridge extension
- Foundry state (hooks, learnings, metrics)
- Local skills
- OpenClaw config, identity, credentials, telegram state, devices, cron, subagent registry
- SQLite memory DB

### Restore docs
- `backup-snapshots/2026-03-23/README-RESTORE.md`
- `backup-snapshots/2026-03-23/SNAPSHOT_SUMMARY.md`
- `backup-snapshots/2026-03-23/manifests/file-manifest.json`

### Notes
This backup likely contains secrets/tokens and should be treated as highly sensitive.

## 2026-03-23 20:39 UTC — Claude Code bridge investigation
- Vetted external repo `Enderfga/openclaw-claude-code-skill`; verdict: install with caution. It appears to be a client/CLI wrapper that expects a local backend API at `http://127.0.0.1:18795/backend-api/claude-code/*`, not a full standalone bridge.
- Tested expected backend endpoints (`/tools`, `/status`, `/sessions`) via local fetch; all failed because the backend was not reachable.
- Built and ran the skill client; it reported `Status: Not connected` and suggested `claude-code-skill connect`.
- Conclusion at time of test: cannot yet confirm whether the intended Claude bridge uses Anthropic OAuth vs API key because the required local backend route/service was not active.
- Notable finding: local installed code under `lossless-claw` / bundled OpenClaw dependencies includes Anthropic OAuth / Claude Code token handling logic (including detection of OAuth-style Anthropic tokens and Claude Code headers), so the stack appears capable of OAuth-backed Anthropic auth once the backend path is properly wired.
