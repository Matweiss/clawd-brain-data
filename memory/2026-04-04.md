## 2026-04-04

- Anthropic third-party/OpenClaw access cutoff hit; Mat chose to get OpenAI working via ChatGPT OAuth because Sonnet is expected to be removed soon.
- OpenAI Codex OAuth was configured and gateway restarted successfully; main/default model is `openai-codex/gpt-5.4`.
- End-to-end messaging test passed after restart (`test` message received and answered normally).
- Paperclip agent model audit results:
  - Hermes → `openclaw_gateway`, `agentId: "hermes"`, no explicit model pinned in Paperclip
  - Pixel → `openclaw_gateway`, `agentId: "pixel"`, no explicit model pinned
  - Arty → `openclaw_gateway`, `agentId: "sarah"`, no explicit model pinned
  - Sage → `openclaw_gateway`, `agentId: "lifestyle"`, no explicit model pinned
  - Bob → `openclaw_gateway`, `agentId: "builder"`, explicitly pinned to `claude-sonnet-4-6`
- Implication from audit: Hermes/Pixel/Arty/Sage likely inherit underlying OpenClaw agent model selection, while Bob is definitely locked to Sonnet until Paperclip config is changed.
- Mat asked whether all agents (Arty, Sage, Bob, Pixel, Hermes) can be ensured to have GPT-5.4 access; next recommended action is to patch Paperclip configs, preferably explicitly pinning all five to `openai-codex/gpt-5.4` for predictability.
