# 2026-04-13

- Refreshed revoked Google OAuth tokens for `thematweiss@gmail.com`, `sarahmat0816@gmail.com`, and `mat.weiss@lucrasports.com` via `gog auth add`.
- Verified Gmail reads now work on all three accounts.
- Verified Calendar reads work on all three accounts. `thematweiss@gmail.com` and `sarahmat0816@gmail.com` returned no events in the checked window; `mat.weiss@lucrasports.com` returned upcoming Lucra meetings normally.
- Ran a blockers-style smoke test across Gmail + Calendar on all three accounts after refresh, confirming the auth issue is resolved.
- Added `scripts/auth-health-smoke-test.sh`, which checks Gmail + Calendar access for the three critical Google accounts and writes state to `memory/auth-health-state.json`.
- Added a new cron job, `Auth Health Smoke Test`, scheduled every 30 minutes from 7 AM to 10 PM PT to catch revoked Google auth quickly and alert on failures.

- Started splitting Mission Control into two surfaces: default Mat Mission Control and a separate Sarah dashboard mode/route, to stop Sarah business widgets from cluttering Mat's operator dashboard.
