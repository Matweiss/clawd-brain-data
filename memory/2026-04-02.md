# Memory: 2026-04-02

## Regal Scrape — CDP Breakthrough (03:00–03:30 UTC)

**Context:** Pixel's cron jobs were all in `error` state. Mat asked to get Thu–Sun Regal showtimes manually, then asked to persist the process.

**Problem discovered:** VPS IP (2a02:4780...) is Cloudflare-blocked on regmovies.com. `web_fetch` and direct HTTP all return 403/blocked pages. mcporter `mac-chrome` MCP server lists 29 tools but times out on actual tool calls (`take_snapshot` hangs 60s+).

**Solution found:** Mac Chrome exposes CDP port locally on Mac, reverse-SSH-tunneled to VPS on `127.0.0.1:28800`. Raw socket WebSocket connection (no Origin header) bypasses Chrome's origin policy. `curl` to port 28800 works; Python websocket-client fails (adds Origin header). Fix: raw socket handshake.

**Script written:** `/root/.openclaw/workspace/shared/pixel-agent/scripts/regal-cdp-scrape.py`
- Reusable, documented, handles tab discovery
- No Origin header = Chrome accepts it
- Navigate + wait 7s + innerText = full SPA content

**Tonight's scrape result:**
- Thu Apr 2 through Sun Apr 5 — all 4 days scraped
- ~9,000 chars per day, 14–15 movies per day
- Project Hail Mary (IMAX/70mm/LASER), Super Mario Galaxy, The Drama, Hoppers, Ready or Not 2, etc.
- Files written and committed to clawd-mission-control-v2 (commit 80319e2)

**Pixel SOUL.md updated:**
- Added CDP fallback method for Regal scraping
- Added tab discovery command
- Updated error handling: try CDP before giving up

**Cron jobs status:** All 4 Pixel cron jobs still in `error` state. Triggered a manual run of wednesday-schedule at 03:30 UTC. Root cause of error state TBD — likely same chrome-devtools MCP timeout issue. CDP script is the fix path.

## Pending: Pixel Cron Error Investigation
- All 4 schedule cron jobs stuck on `error`
- wednesday-schedule manually triggered — check if it succeeds with CDP fallback
- May need to update Pixel's HEARTBEAT.md to use CDP script path instead of mcporter
