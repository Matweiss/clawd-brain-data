## 2026-03-26 — System optimization pass

### Mission Control improvements completed
- Removed tracked `.next` build artifacts from git index in `clawd-mission-control-v2` so generated Next.js output stops polluting repo hygiene.
- Hardened `src/pages/api/webhook/ha.ts` to fail closed when `HA_WEBHOOK_SECRET` is missing instead of accepting a dangerous default secret.
- Reworked `src/pages/api/system/backup.ts` away from a shell-based `tar` call in the API route; it now emits a manifest-style backup record instead of shelling out.
- Added ESLint to Mission Control with a compatible Next.js / ESLint version pairing.
- Added `.eslintrc.json` and restored working lint/build validation for Mission Control.
- Refactored agent API routes to use shared `src/lib/openclaw-cli.ts` with `execFileSync` argument invocation instead of shell-built `execSync` strings.
- Removed `src/pages/index-old.tsx`.
- Changed `src/pages/api/recommendations.ts` to return explicit empty/live modes rather than falling back to fake calendar events.
- Fixed `AgentCommandCenter` hook ordering so lint passes again.
- Verified `clawd-mission-control-v2` passes both `npm run lint` and `npm run build`.

### Durable OpenClaw source fix completed
- Upstream source clone at `/root/.openclaw/workspace/tmp/openclaw-upstream` now has a committed durable fix for alias-heavy tool schema flattening.
- Commit:
  - `d51dc5f` — `fix(tools): preserve a required alias when flattening union parameter schemas`
- File:
  - `src/agents/pi-tools.schema.ts`

### Remaining warnings / cleanup debt
- Mission Control still has non-blocking lint warnings:
  - several `react-hooks/exhaustive-deps` warnings
  - several `@next/next/no-img-element` warnings
- Workspace still contains unsorted debris / review-needed items such as:
  - `exec-debug-output.txt` (~57MB)
  - `composio-search.txt`
  - `tmp-claude-skill-backend.cjs`
  - `tmp/`
  - `openclaw-nerve-review/`
- These should be archived, deleted, or relocated intentionally after review rather than blindly removed.
