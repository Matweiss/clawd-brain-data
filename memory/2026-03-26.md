## 2026-03-26 — Phase 3 cleanup progress

### Mission Control warning cleanup completed
- Eliminated the remaining ESLint warning set in `clawd-mission-control-v2`.
- Stabilized fetch callbacks with `useCallback` / correct `useEffect` dependencies in:
  - `src/components/BrainDataPanel.tsx`
  - `src/components/MemoryGraph.tsx`
  - `src/components/MemorySuggestions.tsx`
  - `src/components/mobile/MobileMoviesTab.tsx`
  - `src/components/mobile/MobileYogaTab.tsx`
  - `src/components/views/CalendarView.tsx`
  - `src/components/views/PipelineView.tsx`
- Replaced poster `<img>` usage with `next/image` (using explicit dimensions and `unoptimized`) in:
  - `src/components/MovieCard.tsx`
  - `src/components/RegalCard.tsx`
- Verified Mission Control now passes both:
  - `npm run lint` with zero warnings/errors
  - `npm run build`
- Commit:
  - `9390db4` — `Eliminate remaining lint warnings`

### Artifact cleanup snapshot
- Deleted `exec-debug-output.txt` (~55MB debug artifact).
- Deleted empty `composio-search.txt`.
- Moved `tmp-claude-skill-backend.cjs` into quarantine at `quarantine/2026-03-26/tmp-claude-skill-backend.cjs`.
  - It was an ad hoc local Claude OAuth shim reading a token from `/tmp`, so preserving it in quarantine is safer than leaving it active in workspace root.
- Kept `tmp/openclaw-upstream/` because it contains the durable committed OpenClaw source patch (`d51dc5f`).
- Kept `openclaw-nerve-review/` untouched because it is a standalone git repo and needs an explicit review/archive decision.
