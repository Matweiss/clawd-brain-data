## 2026-03-26 — Phase 3 cleanup progress

### Mission Control warning cleanup completed
- Eliminated the remaining ESLint warning set in `clawd-mission-control-v2`.
- Stabilized fetch callbacks with `useCallback` / correct `useEffect` dependencies in:
  - `src/components/BrainDataPanel.tsx`
  - `src/components/MemoryGraph.tsx`
  - `src/components/MemorySuggestions.tsx`
  - `src/components/mobile/MobileMoviesTab.tsx`
  - `src/components/mobile/MobileYogaTab.tsx`
  - `src/components/views/CalendarView.tsx`
  - `src/components/views/PipelineView.tsx`
- Replaced poster `<img>` usage with `next/image` (using explicit dimensions and `unoptimized`) in:
  - `src/components/MovieCard.tsx`
  - `src/components/RegalCard.tsx`
- Verified Mission Control now passes both:
  - `npm run lint` with zero warnings/errors
  - `npm run build`
- Commit:
  - `9390db4` — `Eliminate remaining lint warnings`

### Artifact cleanup snapshot
- Deleted `exec-debug-output.txt` (~55MB debug artifact).
- Deleted empty `composio-search.txt`.
- Moved `tmp-claude-skill-backend.cjs` into quarantine at `quarantine/2026-03-26/tmp-claude-skill-backend.cjs`.
  - It was an ad hoc local Claude OAuth shim reading a token from `/tmp`, so preserving it in quarantine is safer than leaving it active in workspace root.
- Kept `tmp/openclaw-upstream/` because it contains the durable committed OpenClaw source patch (`d51dc5f`).
- Kept `openclaw-nerve-review/` untouched because it is a standalone git repo and needs an explicit review/archive decision.
- Reviewed `openclaw-nerve-review/`: it is an intentional, clean upstream checkout of `daggerhashimoto/openclaw-nerve` (HEAD `4253220`, aligned with `origin/master`), so it should be treated as a preserved reference repo rather than clutter.
- Restored `memory/corepower-schedule-process.md` and `memory/regal-sherman-oaks-process.md` after verifying they were real tracked process docs with no replacement files present.
- Fixed Sarah dashboard send script to use `openclaw` from PATH instead of the stale hardcoded `/usr/local/bin/openclaw` path.
- Validated local OpenClaw CLI help and corrected Sarah dashboard sender syntax from unsupported `openclaw message send --text` to the installed CLI's `openclaw message send --message`.
- Found that Telegram bot tokens already exist in `/root/.openclaw/openclaw.json` under `channels.telegram.accounts`, including account `sarah`; patched `send-dashboard.sh` to explicitly send via `--account sarah` so cron uses the Sarah Telegram bot context instead of the default token-less path.
- Classified new memory skills as intentional but early-stage:
  - `skills/simple-memory/` = working prototype / incomplete packaging
  - `skills/memory-nlp/` = spec-stage skill scaffold
  - `skills/auto-media-archive/` = spec-stage skill scaffold
- Created `shared/sarah-agent/projects/manychat-masterclass-reference.md`, a living ManyChat operator reference for Sarah + Arty based on a transcript of Gannon Meyer's ManyChat strategy video. It translates the core lessons into Sarah-specific use cases, segmentation, human-in-the-loop rules, and a phased build roadmap.
- Created `shared/sarah-agent/projects/sarah-manychat-audit-build-spec.md`, a companion execution doc that defines the current-state audit checklist, recommended data model, tag conventions, priority flows, handoff rules, and immediate next actions for Sarah's ManyChat system.
- Added `shared/sarah-agent/projects/arty-proactive-handoff-policy.md` to capture Mat's explicit instruction that Arty should be proactively informed about Sarah-business-relevant docs, functions, features, enhancements, and workflows, and should proactively surface them to Sarah instead of waiting to rediscover them.
- Created `shared/sarah-agent/projects/arty-current-business-assets-brief.md` as an Arty-facing map of current high-value Sarah business assets, including ManyChat docs, assistant context docs, and dashboard automation status.
- Created `memory/projects/sarah-agent-current-asset-pointers.md` as a durable Sarah-agent memory pointer file so recall surfaces current business-helpful assets before assuming they are missing.
- Updated the Arty handoff policy with a reusable default pattern: asset -> Arty brief -> memory pointer -> exact next action.
- Wrote an explicit live handoff into `shared/sarah-agent/memory/SESSION.md` and `shared/sarah-agent/memory/today.md` so Arty's own startup memory flow now points directly to the ManyChat docs and the current dashboard reliability status.
- Added `shared/sarah-agent/memory/manychat.md` and strengthened the top of `shared/sarah-agent/memory/SESSION.md` with an explicit "do not say no ManyChat doc exists" directive, so ManyChat now lives in Arty's direct shared-memory path instead of only project docs.
- Added `shared/sarah-agent/memory/anti-loop-tool-use.md` and updated Sarah shared-memory startup files so Arty has an explicit anti-loop fallback policy: one memory search per topic, immediate canonical-file fallback, one direct-read attempt per exact path, and no repeated malformed parameter retries.
- Added a schedule-recall rule to `MEMORY.md`: when Mat asks what he has going on today/this week, recall must search both work context and personal event memory (travel, flights, dinners, meetings, reservations), with `memory/march-2026-events.md` as a primary event anchor until external calendar integration exists.
- Added a broader relationship-first recall rule: default to memory-grounded, context-aware answers for non-trivial questions, using generic knowledge only as support when prior conversations, preferences, plans, goals, or constraints should shape the answer.
- Created and expanded the `skills/memory-grounded-planning/` skill into a v2 relationship-first planning/recall skill, with explicit memory-first workflows for schedule questions, prep questions, priorities, and other natural-language requests where persistent context should drive the answer.
- Implemented v3 memory-grounded planning upgrades: added canonical rolling event memory in `memory/upcoming-events.md`, added skill references for event capture, prep-mode enrichment, and general context routing, and updated the skill to capture future dated plans into canonical event memory for stronger personal recall over time.
- Implemented v4 proactive discovery upgrades: added references for proactive cue expansion, travel discovery, and a proactive briefing standard; updated the memory-grounded planning skill to treat small cues as assisted-discovery triggers, ask compact high-value follow-up bundles, and prefer future connected-system retrieval (email confirmations, airline/hotel accounts, reservations) over making Mat manually reconstruct details.
- Created a v5 April project brief + task checklist for dual-account briefing and confirmation mining across personal + Lucra Gmail/Calendar.
- Added an Arty upgrade package: `shared/sarah-agent/projects/arty-memory-reasoning-upgrade-spec.md`, `shared/sarah-agent/projects/arty-test-prompt-memory-reasoning-v1.md`, `shared/sarah-agent/memory/proactive-reasoning-policy.md`, and `shared/sarah-agent/memory/active-recall-sources.md`, plus injected the core rules into Sarah shared-memory startup files.
- Created a reusable Arty live test kit for ManyChat behavior validation: `shared/sarah-agent/projects/arty-live-test-prompt-manychat-v1.md`, `shared/sarah-agent/projects/arty-grading-rubric-v1.md`, and `shared/sarah-agent/projects/arty-test-kit-usage.md`.
- Built `Arty Evolution Pack v1`: core operating policy, failure-handling ladder, response style guide, ManyChat playbook, collector-journey playbook, and eval suite; wired references into Sarah shared-memory startup files so improvement is structural, not prompt-only.
- Patched Arty Evolution Pack with an assertiveness upgrade: stronger default endings, direct next-move phrasing, and eval criteria favoring calm operator confidence over soft optional phrasing.
- Patched Arty tone/meta rules: grounded warmth allowed, theatrical reassurance discouraged, unnecessary internal system notes discouraged, and motto guidance shifted toward steadier lines like "I'll keep the thread" instead of "if the world forgets" phrasing.
