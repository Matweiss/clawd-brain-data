# 2026-03-15

## Infrastructure details captured from Mat

- Domain: thematweiss.com (Namecheap)
- DNS via Cloudflare (`lina.ns.cloudflare.com`, `odin.ns.cloudflare.com`)
- Cloudflare account email: Mat.weiss@att.net
- Cloudflare account ID + zone ID captured in TOOLS.md
- No subdomains set yet
- SSL/TLS mode currently Full

## Runtime environment constraints

- OpenClaw runs in Docker on Hostinger VPS (`srv882799.hstgr.cloud`)
- Home Assistant runs on Raspberry Pi 4
- No additional always-on local machine for reliable reverse tunnel right now

## Browser coworking direction

- Mat wants live Chrome coworking sessions (OpenClaw 3.13 feature)
- Best fit in current architecture: Browser Relay extension flow from local Chrome
- `profile=user` direct attach from VPS is not reliable without a local browser endpoint bridge

## Future plans

- Tesla integration
- Apple Health integration
- Potential dedicated Mac mini later for persistent local bridge

## Browser coworking breakthrough (same day)

- Achieved live browser control from VPS session through Mat's connected Mac node (`openclaw nodes status` showed connected with `browser` capability).
- CorePower page was successfully opened and class schedule snapshot was retrieved from the connected browser context.
- Critical operational rule discovered: extension relay auth on Mac uses the Mac-local gateway token domain, while node-to-VPS auth uses VPS gateway token.
- Durable runbooks created for repeatability:
  - `handoffs/BROWSER_COWORK_SETUP_2026-03-15.md`
  - `handoffs/BROWSER_NODE_ONBOARDING_10_MIN.md`
- Mat preference: keep terminal windows minimized overnight for continuity; wants low-friction, always-available browser coworking setup.
- Tomorrow follow-up: verify full March calendar/events and all March flights are ingested/visible in Mission Control; if missing, implement ingest/sync and add coverage checks.

## Skill security audit + selective install

- Ran full SOP audit on `ArchieIndian/openclaw-superpowers`.
- Verdict: CAUTION; approved selective install (not all-or-nothing).
- Installed now: `skill-vetting`, `prompt-injection-guard`, `context-budget-guard`, `cron-hygiene`, `dangerous-action-guard`.
- Deferred: high-blast-radius or not-needed-now skills (including `workspace-integrity-guardian`, `multi-agent-coordinator`, `workflow-orchestration`, `secrets-hygiene`, `channel-context-bridge`, `obsidian-sync`, `create-skill`).
- Decision record written to `handoffs/SKILL_INSTALL_DECISIONS_2026-03-15.md`.
