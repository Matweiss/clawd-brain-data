## 2026-03-31

### Sonnet broken → fixed (stale session auth override)

**Symptom:** Claude Sonnet not responding via Telegram DM. Gateway was silently falling back to GPT-5.4 / Codex without error to user. Claude Desktop app worked fine (confirmed not an account usage/rate limit issue).

**Root cause:** Session-scoped stale auth override in:
`/root/.openclaw/agents/main/sessions/sessions.json`
- `authProfileOverride: "openai-codex:default"` (should have been cleared)
- `model: "gpt-5.4"` (pinned by a previous fallback event, never reset)

**Fix applied:**
Removed these fields from `agent:main:telegram:direct:8001393940` session entry:
- `authProfileOverride`
- `authProfileOverrideSource`
- `authProfileOverrideCompactionCount`
- `model` (was gpt-5.4)

No restart required — picked up on next message.

**Backup created:** `/root/.openclaw/agents/main/sessions/sessions.json.bak-2026-03-31T214152Z`

---

### SOP: When Sonnet (or any primary model) stops working

**Signs:**
- Messages still delivering but replies feel different (faster/shorter/less capable)
- `/status` shows unexpected model (e.g. gpt-5.4 when Sonnet should be primary)
- You see 503 / auth errors in logs but responses still come (fallback silently activated)
- Claude Desktop works fine but Clawd doesn't

**Step 1 — Verify what model is actually being used:**
```bash
python3 - <<'PY'
import json
p='/root/.openclaw/agents/main/sessions/sessions.json'
with open(p) as f: data=json.load(f)
k='agent:main:telegram:direct:8001393940'
e=data.get(k,{})
print(json.dumps({x:e.get(x) for x in ['model','provider','authProfileOverride','authProfileOverrideSource','authProfileOverrideCompactionCount']}, indent=2))
PY
```

**Step 2 — Clear the stale override if present:**
```bash
python3 - <<'PY'
import json, shutil, datetime
p='/root/.openclaw/agents/main/sessions/sessions.json'
with open(p) as f: data=json.load(f)
k='agent:main:telegram:direct:8001393940'
e=data[k]
shutil.copy2(p, p + '.bak-' + datetime.datetime.now(datetime.UTC).strftime('%Y-%m-%dT%H%M%SZ'))
for field in ['authProfileOverride','authProfileOverrideSource','authProfileOverrideCompactionCount','model','provider']:
    e.pop(field, None)
with open(p,'w') as f: json.dump(data,f,indent=2); f.write('\n')
print('Cleared. Send a test message — no restart needed.')
PY
```

**Step 3 — If still broken after clearing session, check auth profile:**
```bash
cat /root/.openclaw/agents/main/agent/auth-profiles.json
```
Look for `anthropic:tmw` or `anthropic:default`. If missing or token looks truncated/stale, re-run:
```bash
openclaw models auth setup-token
```

**Step 4 — If 503 / rate limit is the actual issue (not session override):**
Force a temporary model switch with:
```
/model openai-codex/gpt-5.4
```
Then switch back when Anthropic recovers:
```
/model anthropic/claude-sonnet-4-6
```

---

### Model fallback config (as of 2026-03-31)
- **main agent default:** `anthropic/claude-sonnet-4-6` → `openai-codex/gpt-5.4` → `kimi/kimi-for-coding`
- **sarah/lucra/lifestyle:** `anthropic/claude-sonnet-4-6` → `openai-codex/gpt-5.4`
- **builder:** `openai-codex/gpt-5.3-codex` → `openai-codex/gpt-5.4` → `anthropic/claude-sonnet-4-6`
## 2026-03-31

### Sonnet broken → fixed (stale session auth override)

**Symptom:** Claude Sonnet not responding via Telegram DM. Gateway was silently falling back to GPT-5.4 / Codex without error to user. Claude Desktop app worked fine (confirmed not an account usage/rate limit issue).

**Root cause:** Session-scoped stale auth override in:
`/root/.openclaw/agents/main/sessions/sessions.json`
- `authProfileOverride: "openai-codex:default"` (should have been cleared)
- `model: "gpt-5.4"` (pinned by a previous fallback event, never reset)

**Fix applied:**
Removed these fields from `agent:main:telegram:direct:8001393940` session entry:
- `authProfileOverride`
- `authProfileOverrideSource`
- `authProfileOverrideCompactionCount`
- `model` (was gpt-5.4)

No restart required — picked up on next message.

**Backup created:** `/root/.openclaw/agents/main/sessions/sessions.json.bak-2026-03-31T214152Z`

---

### SOP: When Sonnet (or any primary model) stops working

**Signs:**
- Messages still delivering but replies feel different (faster/shorter/less capable)
- `/status` shows unexpected model (e.g. gpt-5.4 when Sonnet should be primary)
- You see 503 / auth errors in logs but responses still come (fallback silently activated)
- Claude Desktop works fine but Clawd doesn't

**Step 1 — Verify what model is actually being used:**
```bash
python3 - <<'PY'
import json
p='/root/.openclaw/agents/main/sessions/sessions.json'
with open(p) as f: data=json.load(f)
k='agent:main:telegram:direct:8001393940'
e=data.get(k,{})
print(json.dumps({x:e.get(x) for x in ['model','provider','authProfileOverride','authProfileOverrideSource','authProfileOverrideCompactionCount']}, indent=2))
PY
```

**Step 2 — Clear the stale override if present:**
```bash
python3 - <<'PY'
import json, shutil, datetime
p='/root/.openclaw/agents/main/sessions/sessions.json'
with open(p) as f: data=json.load(f)
k='agent:main:telegram:direct:8001393940'
e=data[k]
shutil.copy2(p, p + '.bak-' + datetime.datetime.now(datetime.UTC).strftime('%Y-%m-%dT%H%M%SZ'))
for field in ['authProfileOverride','authProfileOverrideSource','authProfileOverrideCompactionCount','model','provider']:
    e.pop(field, None)
with open(p,'w') as f: json.dump(data,f,indent=2); f.write('\n')
print('Cleared. Send a test message — no restart needed.')
PY
```

**Step 3 — If still broken after clearing session, check auth profile:**
```bash
cat /root/.openclaw/agents/main/agent/auth-profiles.json
```
Look for `anthropic:tmw` or `anthropic:default`. If missing or token looks truncated/stale, re-run:
```bash
openclaw models auth setup-token
```

**Step 4 — If 503 / rate limit is the actual issue (not session override):**
Force a temporary model switch with:
```
/model openai-codex/gpt-5.4
```
Then switch back when Anthropic recovers:
```
/model anthropic/claude-sonnet-4-6
```

---

### Model fallback config (as of 2026-03-31)
- **main agent default:** `anthropic/claude-sonnet-4-6` → `openai-codex/gpt-5.4` → `kimi/kimi-for-coding`
- **sarah/lucra/lifestyle:** `anthropic/claude-sonnet-4-6` → `openai-codex/gpt-5.4`
- **builder:** `openai-codex/gpt-5.3-codex` → `openai-codex/gpt-5.4` → `anthropic/claude-sonnet-4-6`

---

### Pixel Agent — Full Setup Completed (2026-03-31 late night)

Pixel is the browser scraper agent. Runs on a fixed PT cron schedule, scrapes CorePower + Regal, writes to Mission Control.

**Files written/fixed:**
- `shared/pixel-agent/SOUL.md` — full scrape workflow, error handling, output paths, guardrails
- `shared/pixel-agent/HEARTBEAT.md` — 4-run cron schedule with prereq checks
- `shared/pixel-agent/TOOLS.md` — mcporter commands, target URLs, CorePower filter details, output paths
- `shared/pixel-agent/memory/errors/` — error log directory created

**Cron jobs fixed (all 4 were in error state):**
- Root causes: invalid Telegram delivery target (`@heartbeat`), bad kimi model fallbacks
- All 4 now: agent=pixel, model=sonnet-4-6, no-deliver (Pixel messages Mat directly)

**Cron schedule:**
| Job | PT time | Coverage |
|-----|---------|----------|
| monday-schedule | Mon 6:00 AM | Mon/Tue/Wed |
| wednesday-schedule | Wed 11:45 PM | Thu/Fri |
| friday-schedule | Fri 11:45 PM | Sat/Sun |
| sunday-schedule | Sun 11:45 PM | Mon/Tue preview |

**Output files:**
- Primary: `clawd-mission-control-v2/data/schedule-current.json`
- Memory: `clawd-mission-control-v2/memory/data/schedule-current.md`

**Browser path:** Mac node (Mat's MacBook Pro) → chrome-devtools MCP (29 tools)

---

### Agent Capabilities — All 8 Filled In (2026-03-31)

All agents in Paperclip dashboard had empty capabilities fields. Filled in:

| Agent | Role summary |
|-------|-------------|
| Clawd | Chief of Staff — routing, memory, Google Workspace, GitHub, HA |
| Arty | Sarah's business — Shopify, orders, art content, Etsy |
| Luke | Lucra sales — briefings, pipeline, battle cards, follow-ups |
| Sage | Lifestyle — workouts, CorePower, personal calendar, household |
| Bob | Builder — skills, extensions, coding, Vercel deploys |
| Scout | Research — company intel, competitive briefs, deep dives |
| Pixel | Browser scraper — CorePower + Regal schedules → Mission Control |
| Hermes | Email + calendar — monitors 3 inboxes, drafts, routes, never sends |
