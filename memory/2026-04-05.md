# Memory Log - 2026-04-05

## Paperclip Non-Root Migration

**Status**: Partially complete - blocked on LLM auth

### Completed
- Migrated Paperclip service from root to non-root user
- Moved state from `/root/.paperclip/` to `/var/lib/paperclip/.paperclip/`
- Updated all config paths (database, logs, storage, secrets)
- Service now runs with `DynamicUser=yes` and proper state directory

### Blocker
Paperclip startup doctor fails on Claude LLM provider validation:
- Current API key in config returns 401
- Mat prefers OAuth over API keys for Claude
- Need to configure Paperclip to use OAuth flow instead of API key

### Next Steps
1. Research Paperclip's OAuth options for Claude
2. Configure OAuth-based authentication
3. Restart Paperclip service
4. Verify Sloan/Vandalay heartbeat flow works

---

## Notes
- Non-root migration infrastructure is complete
- Only remaining issue is LLM auth configuration
- Paperclip config location: `/var/lib/paperclip/.paperclip/instances/default/config.json`
